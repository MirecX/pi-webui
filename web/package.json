{
  "name": "pi-webui-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "TypeScript frontend for pi-webui (streaming transcript + send box)",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/index.html dist/index.html && cp src/app.css dist/app.css"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
